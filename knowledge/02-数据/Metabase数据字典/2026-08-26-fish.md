---
type: metabase_schema_dictionary
date: 2026-08-26
schema: fish
status: observed_metadata
source_engine: mysql_style_information_schema_export
---

# Metabase 数据字典｜fish

> 证据边界：本分册来自可见 `information_schema` 定义。字段值、默认值、连接参数、敏感字段原文均未保存；字段用途和数据层标记中“命名推断”均需由业务 Owner 或数据开发补证。

## 1. Schema 概览

- 表：**188** 张；字段：**3,490** 个。
- 数据层：业务表（事实/维度待补证）。
- 外键、分区、索引明细、行数、更新时刻、保留周期：本次未导出，不能推断。

## 2. 表清单

| 表 | 类型 | 字段数 | 业务域（命名推断） | 受控字段候选数 | 表说明 |
|---|---|---:|---|---:|---|
| `log20260323` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260324` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260325` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260326` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260327` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260328` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260329` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260330` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260331` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260401` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260402` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260403` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260404` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260405` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260406` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260407` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260408` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260409` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260410` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260411` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260412` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260413` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260414` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260415` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260416` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260417` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260418` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260419` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260420` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260421` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260422` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260423` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260424` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260425` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260426` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260427` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260428` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260429` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260430` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260501` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260502` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260503` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260504` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260505` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260506` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260507` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260508` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260509` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260510` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260511` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260512` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260513` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260514` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260515` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260516` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260517` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260518` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260519` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260520` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260521` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260522` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260523` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260524` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260525` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260526` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260527` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260528` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260529` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260530` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260531` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260601` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260602` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260603` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260604` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260605` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260606` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260607` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260608` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260609` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260610` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260611` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260612` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260613` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260614` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260615` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260616` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260617` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260618` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260619` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260620` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260621` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260622` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260623` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260624` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260625` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260626` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260627` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260628` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260629` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260630` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260701` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260702` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260703` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260704` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260705` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260706` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260707` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260708` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260709` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260710` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260711` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260712` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260713` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260714` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260715` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260716` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260717` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260718` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260719` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260720` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260721` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260722` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260723` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260724` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260725` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260726` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260727` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260728` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260729` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260730` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260731` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260801` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260802` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260803` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260804` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260805` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260806` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260807` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260808` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260809` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260810` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260811` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260812` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260813` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260814` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260815` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260816` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260817` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260818` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260819` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260820` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260821` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260822` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260823` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260824` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260825` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `log20260826` | BASE TABLE | 20 | 日志 / 数据质量 | 0 | — |
| `t_activity` | BASE TABLE | 7 | 游戏 / 对局 / RTP | 0 | — |
| `t_ad_click` | BASE TABLE | 8 | 游戏 / 对局 / RTP | 0 | — |
| `t_applicate_friend` | BASE TABLE | 4 | 游戏 / 对局 / RTP | 0 | — |
| `t_arena_player` | BASE TABLE | 15 | 游戏 / 对局 / RTP | 0 | — |
| `t_benefit_record` | BASE TABLE | 4 | 游戏 / 对局 / RTP | 0 | — |
| `t_bet_record` | BASE TABLE | 14 | 游戏 / 对局 / RTP | 0 | — |
| `t_block_word` | BASE TABLE | 6 | 游戏 / 对局 / RTP | 0 | — |
| `t_config` | BASE TABLE | 6 | 配置 / 平台 | 0 | — |
| `t_credit_record` | BASE TABLE | 13 | 游戏 / 对局 / RTP | 0 | — |
| `t_customer_service` | BASE TABLE | 5 | 游戏 / 对局 / RTP | 1 | — |
| `t_data_record` | BASE TABLE | 19 | 游戏 / 对局 / RTP | 0 | — |
| `t_exchange_inventory` | BASE TABLE | 2 | 游戏 / 对局 / RTP | 0 | — |
| `t_exchange_order` | BASE TABLE | 10 | 支付 / 提现 | 0 | — |
| `t_fish_record` | BASE TABLE | 11 | 游戏 / 对局 / RTP | 0 | — |
| `t_friend` | BASE TABLE | 4 | 游戏 / 对局 / RTP | 0 | — |
| `t_invite_quest` | BASE TABLE | 8 | 游戏 / 对局 / RTP | 0 | — |
| `t_mail` | BASE TABLE | 10 | 运营 / 消息 | 0 | — |
| `t_match_record` | BASE TABLE | 8 | 游戏 / 对局 / RTP | 0 | — |
| `t_order` | BASE TABLE | 14 | 支付 / 提现 | 0 | — |
| `t_past_data` | BASE TABLE | 4 | 游戏 / 对局 / RTP | 0 | — |
| `t_phone` | BASE TABLE | 9 | 游戏 / 对局 / RTP | 3 | — |
| `t_phone_card` | BASE TABLE | 11 | 游戏 / 对局 / RTP | 0 | — |
| `t_platform_exchange_order` | BASE TABLE | 12 | 支付 / 提现 | 0 | — |
| `t_platform_order` | BASE TABLE | 12 | 支付 / 提现 | 0 | — |
| `t_player` | BASE TABLE | 70 | 游戏 / 对局 / RTP | 5 | — |
| `t_rank` | BASE TABLE | 4 | 游戏 / 对局 / RTP | 0 | — |
| `t_redeem_code` | BASE TABLE | 5 | 游戏 / 对局 / RTP | 0 | — |
| `t_redeem_code_template` | BASE TABLE | 7 | 游戏 / 对局 / RTP | 0 | — |
| `t_redeem_gift` | BASE TABLE | 4 | 游戏 / 对局 / RTP | 0 | — |
| `t_system` | BASE TABLE | 31 | 游戏 / 对局 / RTP | 3 | — |
| `t_user` | BASE TABLE | 13 | 用户 / 账号 | 4 | — |

## 3. 逐表字段定义

### log20260323

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260324

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260325

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260326

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260327

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260328

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260329

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260330

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260331

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260401

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260402

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260403

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260404

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260405

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260406

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260407

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260408

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260409

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260410

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260411

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260412

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260413

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260414

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260415

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260416

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260417

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260418

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260419

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260420

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260421

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260422

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260423

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260424

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260425

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260426

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260427

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260428

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260429

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260430

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260501

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260502

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260503

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260504

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260505

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260506

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260507

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260508

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260509

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260510

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260511

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260512

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260513

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260514

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260515

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260516

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260517

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260518

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260519

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260520

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260521

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260522

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260523

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260524

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260525

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260526

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260527

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260528

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260529

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260530

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260531

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260601

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260602

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260603

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260604

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260605

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260606

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260607

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260608

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260609

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260610

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260611

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260612

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260613

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260614

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260615

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260616

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260617

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260618

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260619

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260620

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260621

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260622

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260623

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260624

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260625

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260626

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260627

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260628

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260629

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260630

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260701

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260702

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260703

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260704

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260705

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260706

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260707

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260708

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260709

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260710

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260711

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260712

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260713

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260714

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260715

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260716

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260717

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260718

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260719

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260720

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260721

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260722

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260723

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260724

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260725

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260726

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260727

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260728

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260729

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260730

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260731

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260801

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260802

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260803

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260804

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260805

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260806

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260807

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260808

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260809

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260810

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260811

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260812

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260813

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260814

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260815

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260816

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260817

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260818

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260819

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260820

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260821

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260822

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260823

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260824

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260825

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### log20260826

- 表类型：`BASE TABLE`；字段数：20；数据层：业务表（事实/维度待补证）。
- 业务域：日志 / 数据质量（`inferred_name_only`）；建议：入库延迟、错误、重试和数据质量排障；需明确日志保留周期。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `log_type` | `int` | NO | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `log_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `user_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `vip_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `reason` | `int` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `game` | `varchar(20)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `param1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `param2` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `param3` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `param4` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `roomId` | `int` | YES | `MUL` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `str1` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `str2` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `room_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `room_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `param5` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `param6` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_activity

- 表类型：`BASE TABLE`；字段数：7；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `act_params` | `text` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `act_type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `create_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `level` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `start_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `stop_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_ad_click

- 表类型：`BASE TABLE`；字段数：8；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `android_id` | `varchar(255)` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `callback` | `varchar(4096)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `create_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `idfa` | `varchar(255)` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `imei` | `varchar(255)` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `platform` | `varchar(255)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `role_id` | `bigint` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_applicate_friend

- 表类型：`BASE TABLE`；字段数：4；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `varchar(255)` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `role_id1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `role_id2` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_arena_player

- 表类型：`BASE TABLE`；字段数：15；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `current_score` | `float` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `finish_millis` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `high_score` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `name` | `varchar(50)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `arena_rank` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `remain_number` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `score_bonus` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `skill_open` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `three_point_rank` | `varchar(256)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `times` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `version` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `week_version` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `yes_high_score` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `yes_rank` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_benefit_record

- 表类型：`BASE TABLE`；字段数：4；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `timestamp` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `total_cost` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `total_gain` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_bet_record

- 表类型：`BASE TABLE`；字段数：14；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `bet_amount` | `int` | YES | `NONE` | 金额 / 资产 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `catch_fishes` | `varchar(1024)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `com_id` | `varchar(255)` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `currency` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `gain_amount` | `int` | YES | `NONE` | 金额 / 资产 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `hit_fishes` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `ip` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `owner_id` | `varchar(255)` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `parent_id` | `varchar(255)` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `round_id` | `varchar(255)` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `timestamp` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `user_id` | `varchar(255)` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_block_word

- 表类型：`BASE TABLE`；字段数：6；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `block_word` | `varchar(255)` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `param` | `varchar(256)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `replace_word` | `varchar(255)` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `state` | `int` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `update_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_config

- 表类型：`BASE TABLE`；字段数：6；数据层：业务表（事实/维度待补证）。
- 业务域：配置 / 平台（`inferred_name_only`）；建议：配置、版本、字典和开关关联；不作为主统计事实源。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `type` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `type_desc` | `varchar(255)` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `type_id` | `int` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `type_name` | `varchar(255)` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `value` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_credit_record

- 表类型：`BASE TABLE`；字段数：13；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `txid` | `varchar(255)` | NO | `PRI` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `after_point` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `amount` | `int` | YES | `NONE` | 金额 / 资产 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `before_point` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `currency` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `ip` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `ptxid` | `varchar(255)` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `self_role_id` | `bigint` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `self_role_name` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `self_user_id` | `bigint` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `timestamp` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `type` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `user_id` | `varchar(255)` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_customer_service

- 表类型：`BASE TABLE`；字段数：5；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `varchar(255)` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `create_time_millis` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `bigint` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 4 | `qq` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `state` | `int` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_data_record

- 表类型：`BASE TABLE`；字段数：19；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `arm_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `card_arm_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `card_probability` | `double` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `card_random` | `double` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `drop_item` | `varchar(512)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `drop_random` | `double` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `energy` | `float` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `fish_score` | `float` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `fish_temp_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `fish_value` | `float` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `give_gold` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `is_have_game_card` | `int` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `multiple_energy` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `probability` | `double` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `random` | `double` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `role_id` | `bigint` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `update_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_exchange_inventory

- 表类型：`BASE TABLE`；字段数：2；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `number` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_exchange_order

- 表类型：`BASE TABLE`；字段数：10；数据层：业务表（事实/维度待补证）。
- 业务域：支付 / 提现（`inferred_name_only`）；建议：支付/提现漏斗、成功率、时延与失败码；金融金额以服务端事实复核。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `after_exchange` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `before_exchange` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `content` | `varchar(256)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `item_id` | `int` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `number` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `order_id` | `varchar(255)` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `param` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `result` | `varchar(255)` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `role_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_fish_record

- 表类型：`BASE TABLE`；字段数：11；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `cash` | `bigint` | YES | `NONE` | 金额 / 资产 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `chip` | `bigint` | YES | `NONE` | 金额 / 资产 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `cost` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `fire_level` | `int` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `gain` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `multiply` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `record_type` | `int` | YES | `MUL` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `record_type_id` | `int` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `timestamp` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `user_id` | `bigint` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_friend

- 表类型：`BASE TABLE`；字段数：4；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `varchar(255)` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `role_id1` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `role_id2` | `bigint` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_invite_quest

- 表类型：`BASE TABLE`；字段数：8；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `invite_role_id` | `bigint` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `is_finish_be_invite` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `is_meet` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `new_or_old` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `quest_data` | `text` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `round` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `week_version` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_mail

- 表类型：`BASE TABLE`；字段数：10；数据层：业务表（事实/维度待补证）。
- 业务域：运营 / 消息（`inferred_name_only`）；建议：消息触达、发送结果与运营触发分析；不得输出接收人身份字段。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `bonuses` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `expire_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `param` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `role_id` | `bigint` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `send_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `sender` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `sender_name` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `state` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `type` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_match_record

- 表类型：`BASE TABLE`；字段数：8；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `cost_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `damage` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `hp_max` | `bigint` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `killer_id` | `bigint` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `match_id` | `int` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `rank_snap` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_order

- 表类型：`BASE TABLE`；字段数：14；数据层：业务表（事实/维度待补证）。
- 业务域：支付 / 提现（`inferred_name_only`）；建议：支付/提现漏斗、成功率、时延与失败码；金融金额以服务端事实复核。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `varchar(255)` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `charge_id` | `int` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `create_time` | `bigint` | YES | `MUL` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `finish_time` | `bigint` | YES | `MUL` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `order_data` | `varchar(1024)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `pay_item` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `platform_id` | `varchar(255)` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `platform_order_id` | `varchar(255)` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `platform_uid` | `varchar(255)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `player_level` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `rmb` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `role_id` | `bigint` | NO | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `state` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `vip_level` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_past_data

- 表类型：`BASE TABLE`；字段数：4；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `date_str` | `bigint` | NO | `PRI` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `type` | `int` | NO | `PRI` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `rank_content` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_phone

- 表类型：`BASE TABLE`；字段数：9；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | [REDACTED:CREDENTIAL_OR_SECRET] | `bigint` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 禁止展示、导出或进入分析产物 |
| 3 | `code` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `code_times` | `int` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `code_valid_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `device_id` | `varchar(255)` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | [REDACTED:CREDENTIAL_OR_SECRET] | `varchar(255)` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 禁止展示、导出或进入分析产物 |
| 8 | [REDACTED:CREDENTIAL_OR_SECRET] | `int` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 禁止展示、导出或进入分析产物 |
| 9 | `version` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_phone_card

- 表类型：`BASE TABLE`；字段数：11；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `operator` | `varchar(255)` | NO | `PRI` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `serial` | `varchar(255)` | NO | `PRI` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `create_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `exchange_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `num_id` | `int` | NO | `UNI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `param` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `pin` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `role_id` | `bigint` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `status` | `int` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `valid_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `value` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_platform_exchange_order

- 表类型：`BASE TABLE`；字段数：12；数据层：业务表（事实/维度待补证）。
- 业务域：支付 / 提现（`inferred_name_only`）；建议：支付/提现漏斗、成功率、时延与失败码；金融金额以服务端事实复核。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `after_exchange` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `before_exchange` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `content` | `varchar(256)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `item_id` | `int` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `number` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `order_id` | `varchar(255)` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `param` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `platform_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `platform_ip` | `varchar(255)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `result` | `varchar(255)` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `role_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_platform_order

- 表类型：`BASE TABLE`；字段数：12；数据层：业务表（事实/维度待补证）。
- 业务域：支付 / 提现（`inferred_name_only`）；建议：支付/提现漏斗、成功率、时延与失败码；金融金额以服务端事实复核。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `varchar(255)` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `account_id` | `varchar(255)` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `call_back_data` | `varchar(1024)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `create_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `finish_time` | `bigint` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `param` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `param_int` | `int` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `platform_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `platform_ip` | `varchar(255)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `result_code` | `varchar(255)` | YES | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `role_id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `status` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_player

- 表类型：`BASE TABLE`；字段数：70；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `activity_data` | `text` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `ad_click_id` | `int` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `ad_free_gold` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `add_lock` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `arm_avatar` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `auto_fire_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `bless_fish_count` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `broke_cd` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `broke_room_limit` | `bigint` | NO | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `broke_version` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `cannons` | `varchar(256)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `channel` | `varchar(32)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `charge_cards` | `varchar(255)` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `charge_pool` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `check_in` | `varchar(255)` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `coupon` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `create_time` | `bigint` | NO | `MUL` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `daily_charge` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `daily_data` | `varchar(512)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 21 | `daily_liviness` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 22 | `diamond` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 23 | `exchange` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 24 | `exchange_version` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 25 | `exp` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 26 | `extra_coupon` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 27 | `fame` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 28 | `flag` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 29 | `game_cards` | `varchar(255)` | NO | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 30 | `game_time` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 31 | `gold` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 32 | `growth_fund` | `varchar(256)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 33 | `icon` | `varchar(512)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 34 | `invite_data` | `varchar(256)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 35 | `ip` | `varchar(32)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 36 | `items` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 37 | `last_login_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 38 | `level` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 39 | `login_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 40 | `max_arm_level` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 41 | `name` | `varchar(50)` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 42 | `offline_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 43 | `param_data` | `text` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 44 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `bigint` | NO | `MUL` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 45 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `bigint` | NO | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 46 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `float` | NO | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 47 | `platform_icon` | `varchar(512)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 48 | `real_card` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 49 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `varchar(255)` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 50 | `role` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 51 | `role_rank` | `text` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 52 | `role_task` | `text` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 53 | `room_basic_drop` | `varchar(256)` | YES | `NONE` | 游戏 / 玩法 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 54 | `server_mails` | `varchar(256)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 55 | `sex` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 56 | `share_bonus` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 57 | `shop_items` | `varchar(255)` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 58 | `sky_drop_times` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 59 | `statistics_data` | `text` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 60 | `system_info` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 61 | `time_limit` | `varchar(256)` | YES | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 62 | `today_shared` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 63 | [REDACTED:CREDENTIAL_OR_SECRET] | `varchar(255)` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 禁止展示、导出或进入分析产物 |
| 64 | `update_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 65 | `user_id` | `bigint` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 66 | `vip_critical_day` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 67 | `vip_exp` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 68 | `vip_level` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 69 | `vip_version` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 70 | `week_liviness` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_rank

- 表类型：`BASE TABLE`；字段数：4；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `date_version` | `int` | NO | `PRI` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `rank_type` | `int` | NO | `PRI` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `id` | `bigint` | NO | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `rank_content` | `text` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_redeem_code

- 表类型：`BASE TABLE`；字段数：5；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `redeem_id` | `varchar(255)` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `count` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `create_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `param` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `state` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_redeem_code_template

- 表类型：`BASE TABLE`；字段数：7；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `limits` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `named` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `prize1` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `repeats` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `usages` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `validity` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_redeem_gift

- 表类型：`BASE TABLE`；字段数：4；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `create_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `gift_id` | `int` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `param` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_system

- 表类型：`BASE TABLE`；字段数：31；数据层：业务表（事实/维度待补证）。
- 业务域：游戏 / 对局 / RTP（`inferred_name_only`）；建议：游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `int` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `allow_role` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `android_limit` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | `android_limit_new` | `bigint` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 5 | `android_limit_new_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 6 | `channel_rate` | `varchar(256)` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `forbid_city_lv` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | `forbid_city_start_hour` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 9 | `forbid_city_stop_hour` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | `forbid_citys` | `varchar(255)` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 11 | `fuli` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `function_mask` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `g_forbid_city_lv` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 14 | `g_forbid_city_start_hour` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 15 | `g_forbid_city_stop_hour` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 16 | `g_forbid_citys` | `varchar(255)` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 17 | `ios_level` | `int` | NO | `NONE` | 状态 / 枚举 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 18 | `ios_start_hour` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 19 | `ios_stop_hour` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 20 | `ios_vip_exp` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 21 | `money_limit` | `int` | NO | `NONE` | 金额 / 资产 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 22 | `national_day_time` | `varchar(255)` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 23 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `varchar(255)` | NO | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 24 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `int` | NO | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 25 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `varchar(255)` | NO | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 26 | `planbtime` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 27 | `recommendation_list` | `varchar(255)` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 28 | `recommendation_rate` | `int` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 29 | `reg_time` | `bigint` | NO | `NONE` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 30 | `server_limit` | `varchar(2048)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 31 | `share_forbid_citys` | `varchar(255)` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |

### t_user

- 表类型：`BASE TABLE`；字段数：13；数据层：业务表（事实/维度待补证）。
- 业务域：用户 / 账号（`inferred_name_only`）；建议：注册、登录与生命周期聚合；禁止导出可识别身份字段。
- 关系状态：主键/普通索引标记来自 `COLUMN_KEY`；外键关系未导出，不能据字段命名假定关联。

| 序号 | 字段 | 类型 | 可空 | 键标记 | 字段分组 | 字段说明 | 使用边界 |
|---:|---|---|---|---|---|---|---|
| 1 | `id` | `bigint` | NO | `PRI` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 2 | `channel` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 3 | `device_id` | `varchar(255)` | YES | `NONE` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 4 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `varchar(255)` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 5 | [REDACTED:KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC] | `varchar(255)` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 受限；仅脱敏聚合或伪标识下钻 |
| 6 | `nickname` | `varchar(255)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 7 | `param` | `varchar(1024)` | YES | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 8 | [REDACTED:CREDENTIAL_OR_SECRET] | `varchar(255)` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 禁止展示、导出或进入分析产物 |
| 9 | `platform_id` | `varchar(255)` | YES | `MUL` | 标识 / 关联 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 10 | [REDACTED:CREDENTIAL_OR_SECRET] | `varchar(255)` | YES | `NONE` | 敏感字段 | [已脱敏：敏感字段说明不进入知识库] | 禁止展示、导出或进入分析产物 |
| 11 | `platform_type` | `varchar(255)` | YES | `MUL` | 时间 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 12 | `role` | `int` | NO | `NONE` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
| 13 | `username` | `varchar(255)` | YES | `MUL` | 业务属性 | — | 允许在授权范围内做聚合分析；不输出字段值 |
