---
type: metabase_schema_inventory
date: 2026-08-26
status: observed_metadata
source_engine: mysql_style_information_schema_export
tags: [Metabase, MySQL, 数据字典, 表结构, 字段治理]
---

# Metabase 全库数据资产索引

> 本资料基于当前账号在 Metabase 原生 SQL 中导出的 `information_schema` 元数据。只描述可见 Schema、表和字段定义；没有读取任何业务数据行、连接配置、凭据或字段值。

## 1. 覆盖范围与证据状态

- 可见 Schema：**6** 个；表：**797** 张；字段：**12,251** 个。
- 表类型：`BASE TABLE` 797；本次未观察到视图。
- 证据状态：`observed_metadata` 仅表示字段定义可见，不表示字段已有值、数据新鲜、口径认证或可直接用于正式报表。
- 数据库引擎：导出结构符合 MySQL `information_schema`；Metabase 的连接引擎配置本身未读取，保留为 `engine_inference`。

## 2. Schema 覆盖

| Schema | 表数 | 字段数 | 数据层（命名推断） | 阅读版 |
|---|---:|---:|---|---|
| `ApolloConfigDB` | 17 | 180 | 配置元数据（命名推断） | [[Metabase数据字典/2026-08-26-apolloconfigdb]] |
| `ApolloPortalDB` | 16 | 126 | 配置元数据（命名推断） | [[Metabase数据字典/2026-08-26-apolloportaldb]] |
| `fish` | 188 | 3,490 | 业务表（事实/维度待补证） | [[Metabase数据字典/2026-08-26-fish]] |
| `log` | 176 | 3,520 | 日志 / 审计（命名推断） | [[Metabase数据字典/2026-08-26-log]] |
| `wajecall` | 155 | 2,338 | 业务表（事实/维度待补证） | [[Metabase数据字典/2026-08-26-wajecall]] |
| `whot_center` | 245 | 2,597 | 业务表（事实/维度待补证） | [[Metabase数据字典/2026-08-26-whot-center]] |

## 3. 结构完整性与可用性

- 表—字段可对账：**797/797** 张表存在字段记录；缺口为 **0**。
- 主键标记：797 张表至少有一个 `PRI` 字段；字段级索引标记为 `PRI` 804、`UNI` 72、`MUL` 2,354。
- 注释覆盖：表说明 46.8%；字段说明 34.28%。低注释表和字段需补 Owner、业务口径与保留策略。
- 本次未收集：外键约束、索引明细、分区、DDL、行数、数据新鲜度与字段实际值；这些均不得由本索引推断。

## 4. 业务域候选分布（命名推断）

| 业务域 | 表数 | 推荐分析用途 |
|---|---:|---|
| 日志 / 数据质量 | 363 | 入库延迟、错误、重试和数据质量排障；需明确日志保留周期。 |
| 待分类 | 193 | 仅作为候选资产；确认业务 Owner 与事实口径后再接入分析。 |
| 用户 / 账号 | 73 | 注册、登录与生命周期聚合；禁止导出可识别身份字段。 |
| 资产 / 货币 | 52 | 资产流、奖励成本、余额变化与账本对账；必须区分真金、金币和奖励资产。 |
| 配置 / 平台 | 35 | 配置、版本、字典和开关关联；不作为主统计事实源。 |
| 游戏 / 对局 / RTP | 35 | 游戏局、下注、结算、RTP 和玩法/房间拆分；需与 GAMEEND 链路核验。 |
| 支付 / 提现 | 27 | 支付/提现漏斗、成功率、时延与失败码；金融金额以服务端事实复核。 |
| KYC / 风控 | 10 | KYC 漏斗、失败原因、认证时延；仅使用脱敏聚合。 |
| 运营 / 消息 | 9 | 消息触达、发送结果与运营触发分析；不得输出接收人身份字段。 |

## 5. 受控字段治理

| 敏感类别 | 受控字段/注释候选数 | 受影响表数 | 知识库处理 |
|---|---:|---:|---|
| `CREDENTIAL_OR_SECRET` | 20 | 12 | 字段名和说明已替换为类别标签；不输出字段值、默认值或原始响应。 |
| `KYC_PERSONAL_IDENTIFIER_OR_BIOMETRIC` | 89 | 38 | 字段名和说明已替换为类别标签；不输出字段值、默认值或原始响应。 |
| `PAYMENT_ACCOUNT_IDENTIFIER` | 2 | 1 | 字段名和说明已替换为类别标签；不输出字段值、默认值或原始响应。 |
| `THIRD_PARTY_RAW_RESPONSE_CANDIDATE` | 18 | 13 | 字段名和说明已替换为类别标签；不输出字段值、默认值或原始响应。 |

## 6. 与 Waje 数据架构的关系

- Metabase 是受控访问、专题分析和风险隔离层；统计事实仍需以 BigQuery 认证层为准，MySQL 配置表不能单独承担正式经营口径。
- 本次 `whot_center` 是可见 Schema 之一，包含 245 张表与 2,597 个字段；其结构字典见对应分册。
- KYC、支付、资产和用户类表仅可用于脱敏聚合或受控伪标识下钻；不得进入普通看板、公开导出或知识库字段原文。
- 后续要建设 SQL 查询库或 KYC 看板时，先以本字典中的真实表/字段为准，再执行完整日、时区、金额单位与服务端账本对账验证。

## 7. 关联资料

- [[Ares与Metabase看板建设方案对比分析-2026-08-05]]
- [[Metabase-KYC人脸识别看板配置指南-2026-08-19]]
- [[Waje数据指标看板映射与研发验收清单-2026-08-11]]

## 8. 刷新与验收

1. 重新导出同一元数据 SQL 的表清单与字段清单；原始 CSV 保持在项目外部。
2. 用文件哈希和表/字段数量比对新增、删除与类型变化。
3. 仅对需要进入正式分析的表，补采外键、分区、数据新鲜度、完整日和数据质量证据。
4. 若权限或同步状态变化，标记 `blocked_permission` 或 `metadata_stale`，不将其解释为数据为空。
