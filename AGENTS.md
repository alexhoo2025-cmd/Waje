# Waje 项目 Agent 协作入口

## 查询效率与扫描范围（2026-09-10）

所有新建/修订的查询脚本先明确任务窗口和每张表的最小必要日期范围，只选必要字段、下推分区与业务过滤、优先复用已核验结果；禁止默认从历史最早日期扫描至今。注册/支付起点窗口与留存观察窗口分开：8月新增注册用户只查8月起点，回访可延伸至已批准截止日。首次付费可能包含早期注册用户，不可机械截断画像而漏人；必要历史读取须有语义依据、最小化方案与扫描量估算。按年分区不保证按月筛选同幅减少字节，必须用dry-run验证实际扫描量。新增直接客户端入口先用`scripts/check_query_window.py`与相邻`.scope.json`校验日期字面量，再做只读校验与dry-run；该检查不是完整SQL语义分析。旧SQL快照只读保留，不批量改写审计证据。

### BigQuery 默认执行通道（2026-09-14）

Waje 的交互与定时聚合查询默认走**企业 BigQuery API / 客户端库**，不以 Remote MCP 为前置条件。使用项目隔离 Python 环境中带身份核验、`validate_readonly_sql.py`、`check_query_window.py`、dry-run、5 GiB 单查询／25 GiB 任务上限与 job receipt 的受控只读 runner；只允许 `SELECT/WITH`、最小日期分区、聚合输出和最多 3,000 行结果。

Remote MCP 仅用于已通过独立 trust、IAM 和安全 View 白名单门禁的元数据发现或探索性聚合，默认不调用、不重试；其被阻断不能写成“BigQuery 无数据”，也不能阻断已通过 API 预检的固定查询。API 认证、dry-run、执行或覆盖检查失败时如实标记 `blocked`、`partial` 或 `degraded`；不得切换个人账号、放宽 SQL、改用明细输出或补零。完整契约见`knowledge/04-方法/Waje-BigQuery默认API只读查询规范-2026-09-14.md`与`config/bigquery_mcp_policy.json`。

## 分析报告默认入口（2026-09-09）

报告以用户最后修改/确认的版本为内容和排版基准，不能默认本地artifact或生成时间较新的文件更权威。同步前读取指定来源最新全文，保存revision/哈希并比较差异；跨端均有未同步修改时先解决冲突，禁止旧生成器覆盖定稿。流程见`knowledge/05-运行/报告定稿与跨格式同步工作流-2026-09-09.md`。只同步已请求的格式；数据与因果疑点保留证据状态，不因定稿而变为已核验。

新增或修订分析报告，先读`knowledge/05-运行/报告排版与配色规范-2026-09-07.md`，再选`knowledge/90-模板/分析报告/`中的相应骨架。本任务最后确认的人群、起点、端、日期和币种优先，不能把某份报告定义推广为统一口径。

交付前运行`node scripts/check_report_quality.mjs --input <artifact.json|report.md|report.xml>`。HTML继续走`scripts/deliver_readable_report.mjs`，已默认接入检查；`--check-only`不构建或发布。硬错误拦截，风格提示不自动改写。文本检查不证明业务计算正确；仍须核验数据、桌面/窄屏与飞书全文回读。不得为了检查通过修改历史样板或删除必要限制。此规则只作用于Waje项目，不新增例行任务的多Agent调用。

项目是 Waje 产品、数据和市场分析工作区。新任务先使用本 `AGENTS.md`；只有需要项目地图时才按段读取 `CLAUDE.md`，不得把全文读取作为每个任务的默认步骤。续接任务优先读取对应 `analysis/thread_handoffs/<任务>/CURRENT.md` 及其中列出的回执。已有修改属于用户，禁止重置、清理或覆盖无关文件。

## 上下文检查点与换窗

- 每个聊天只承担一个可独立验收的成果；不同报表、平台写入、浏览器流程或代码改造使用不同聊天。
- 当界面显示剩余上下文不高于约 35%、系统即将压缩、任务完成一个关键阶段、即将产生大型 DOM/JSON/表格输出，或需要转交新聊天时，必须先生成检查点。
- 检查点统一写入 `analysis/thread_handoffs/<任务slug>/CURRENT.md`。使用：

```bash
python3 scripts/create_thread_checkpoint.py \
  --task "任务名称" \
  --slug task-slug \
  --objective "本轮具体目标" \
  --scope analysis/相关任务目录 \
  --next "下一步动作"
```

- 生成器只扫描显式 `--scope`，自动汇总 Git 状态、最新回执和主要产物；更新现有 `CURRENT.md` 前会保存历史副本。
- 新聊天只读取 `AGENTS.md`、对应 `CURRENT.md` 和其中列出的最新回执；不得复制、fork 或重新粘贴旧聊天全文。接手后仍须核验当前文件、在线 revision、权限和外部状态。
- 大型网页树、原始 JSON、SQL 结果、表格明细和长日志必须落盘；聊天只保留状态、数量、差异摘要和路径。

## Claude Code Agent 分派已停用（2026-09-15）

用户确认 Claude Code Agent 当前不可用。`config/agent_dispatch.json` 已设为 `enabled: false`：禁止对任何项目任务调用 `tools/claude_bridge.py` 的 `preflight`、`dispatch`、重试、模型升级或回退；桥接代码、测试和历史回执只读保留，不能作为当前可用能力的证据。

复杂交互任务由主 Agent 直接完成；需要企业资料、授权聚合查询或多源研究时，Gemini 网页 Agent 仅在用户已授权、对应企业会话可用且输出可验收时按需使用。本地 Gemini CLI 仍完全排除。简单日常任务继续使用既有脚本和默认轻量模型。

## 简单日常任务排除

Play报告、竞品周报、晨会纪要和配置差异流程继续使用既有脚本与默认模型；不生成 Claude 分派、附加分析或待审任务。历史测试报告中的协作集成不代表当前启用。

## 每周协作审计与记录

每周五的协作审计改为可用通道的观察性审计：只汇总 Gemini 网页 Agent 的最小脱敏回执与主 Agent 交付质量，不调度模型、不修改 Claude 桥接或分派配置。未记录的运行量保持 `unknown`。

## Gemini 各通道

用户已确认Gemini网页Agent执行效果较好，偶有账户/凭证验证需求，保留为复杂任务的可用专项通道（企业资料、授权查询、多源研究等）。遇登录失效恢复同一授权账号后继续，不因此整条停用。该确认是用户反馈，不虚构网页调用次数或本地成功回执。
本地Gemini CLI权限未开通（用户确认），完全排除出调配与协作，不自动调用、重试或做恢复探测；CLI问题不得推及网页Agent。详细评估见analysis/gemini_usage_review_2026_09_04/reviewed-report.md。现有企业身份、查询和数据边界继续生效。
不将企业查询凭据或权限交给第三方服务；Gemini 网页专项也仅接收用户授权范围内的最小必要材料。外部消息、发布和生产写入依照用户已有授权范围执行。

## 登录与授权阻断默认流程

遇到 `auth_required`、登录失效、权限确认或凭证验证时，默认在 Codex 内置浏览器前台打开并保留对应授权页，不先询问是否打开。用户在页面完成密码、验证码或同意操作后，主 Agent 再重新核验用户身份、目标资源和必要 scope，确认通过后继续原任务。

主 Agent 不代填密码、OTP、Token 或 API Key，不保存授权 URL、验证码、Cookie 或凭据，不切换账号、不放宽权限；内置浏览器确实无法承载时，才说明原因并改用用户可见的外部授权页。授权页打开不等于授权完成；用户未完成操作时保持 `auth_required`/`blocked`，不把缺少权限解释为无数据或成功。
