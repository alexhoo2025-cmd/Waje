# Whot 最近 7 个完整业务日分时运营与 RTP 分布分析

本分析包对应窗口 `2026-08-28—2026-09-03`，业务时区 `Africa/Lagos`，游戏范围为精确 `Whot`。预期交付为 1 小时主表、3 小时运营时段汇总、用户 RTP 区间分布、质量回执、本地自包含 HTML 和独立飞书文档。

## 当前结论

本次运行状态为 `partial`：线上 BigQuery 已恢复可读，7/7 个完整业务日的 Whot 服务端聚合已完成；进入事件、金额单位、结算语义和实时并发仍有明确边界，不能把临时 RTP 当作已认证经营指标。

- 企业账号已登录 `wajenigeria` 项目；查询只读、按日期分区并控制在结果上限内。
- 本地游戏字典确认 Whot 的游戏 ID 为 `6001`，事件事实中的精确玩法键为 `play_id=9116001`。未合并 `BaccaWhot` 或 `Whotduel`。
- `realtime_event_server` 的 `GAMEEND` 和 `GAMESTART` 覆盖 `2026-08-28—2026-09-03`，日级和 24×7 小时点均已返回。真人与机器人按 `is_robot` 分开统计。
- 7 日真人下注额为 `121,437,518,200` 个源整数单位，结算现金字段为 `115,622,053,550` 个源整数单位；按 `cash_settlement / bet_num` 得出的金额加权 RTP 为 `95.21%`，但金额单位及该字段是否等价最终派奖尚未由业务口径签字确认，因此报告标注为临时值。
- 机器人下注额为 `52,085,351,300` 个源整数单位，占真人+机器人下注额 `30.02%`；机器人不进入真人主表。每日约 `2,994` 个机器人用户是观察到的机器人池规模，不是实时并发。
- 客户端精确 Whot `PV` 仅返回 `396` 个事件、`264` 个去重用户，不能当作完整进入人数；`MV` 为模块曝光辅助指标。`GAMESTART` 单独展示，不能冒充进入人数。
- 未发现带 Whot 维度的 APPONLINE/session 区间，因此不输出真实最小/最大/中位并发，只使用下注用户作为活跃用户代理。

## 当前仍需补证

1. 认证 `bet_num`、`cash_settlement` 的金额单位、正负号和最终结算语义；`score` 明确不作为派奖字段。
2. 补齐可靠的 Whot 进入事件或页面打开事件，区分 PV、模块曝光和真正进入玩法。
3. 完成 `GAMESTART → GAMEEND → BETREWARD` 事件链路核验；当前精确 Whot 服务端结果未返回独立 `BETREWARD`，因此结算链路为部分覆盖。
4. 如果要做实时匹配容量，补充 Whot 维度的会话开始/结束或心跳区间。报告不会用全 App 在线人数替代 Whot 并发。

## 复核入口

- `data_contract.json`：输出字段、定义、分母和状态语义。
- `source_inventory.json`：数据源、表结构、风险和当前状态。
- `query_manifest.json`：查询顺序、dry run、扫描上限和执行状态。
- `run_receipt.json`：本次运行回执和阻断证据。
- `quality_checks.json`：验收矩阵和未关闭质量门。
- `sql/`：只读聚合 SQL 模板；未授权前不执行生产全量扫描。
- `artifact.json`：canonical portable report 输入；数值均来自线上聚合结果，临时字段和阻断项保留状态。

## 安全边界

查询模板只输出聚合结果，不返回用户键、设备唯一标识、订单明细、完整事件参数、Cookie、Token 或凭据。用户 ID 只能在查询内部用于去重和 RTP 分布分组，并在最终结果中消失。没有稳定关联键、分母为零、权限不足、数据延迟和未观察到分别使用不同状态，不能合并成 `0`。
