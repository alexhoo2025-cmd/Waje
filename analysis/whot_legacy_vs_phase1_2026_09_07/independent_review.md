## Claude 协作分析（主 Agent 已验收）

旧版埋点覆盖入口、开局、结束和资金四类信号，但匹配受理、队列、降级建议、响应、取消与失败终态缺少完整现网证据，不能视为已上线的完整匹配链路。一期01—07在此基础上新增选择与开局分离、房间展示-点击-受理拆分、匹配全生命周期字段和4人降级建议闭环，属于全新设计而非旧版延伸。两版口径不同，不能用旧版GAMESTART或MC数据反推匹配成功率；共用匹配池对比需先固定房间、时段、付费状态、端和版本。建议按入口复用、加载补齐、房间拆分、匹配闭环、降级建议闭环的顺序分步验收上线。

- [fact] 旧版通用链路可观察入口、开局、结束与资金，但缺乏服务端匹配受理、队列、降级建议、响应、取消和失败终态的完整现网上报证据，不应被当作已完整落地的匹配链路。（证据：old-evidence）
- [fact] 一期范围严格限定为01—07七个需求，不含其外任何历史或未来功能，审查须以此为边界。（证据：phase1-scope）
- [fact] 一期设计新增了旧版不具备的精细化字段：匹配全生命周期（请求-状态-取消-终态-GAMESTART关联）和4人降级建议全闭环，这些是一期的必须新增项，非对旧版的简单延续。（证据：phase1-contract）
- [inference] 由于旧版缺少可重建的匹配请求和失败终态字段，旧版与一期在匹配成功率等指标上不可直接比较，用GAMESTART人数或MC点击量推算旧版匹配成功率的做法不成立。（证据：comparison-boundary, old-evidence）
- [recommendation] 迁移验收建议按入口复用（01/02，直接沿用旧版MC/ASSET等可比字段）→加载补齐（03，H5四态start/ready/fail-timeout-abandon/bet_ready）→房间拆分（04/05，展示-点击-受理分层）→匹配闭环（06，MATCH_REQUEST到终态与GAMESTART关联）→降级建议闭环（07，N1/N2签发到降级开局）顺序分步上线验收；每步验收前须先固定房间、时段、付费状态、端和版本口径，避免共用匹配池数据串扰。（证据：comparison-boundary, phase1-contract）

待确认：一期01—07目前是仅完成设计契约，还是已进入开发、灰度或生产验证阶段？；旧版历史统计需求所覆盖的时间范围与现网数据仓库的实际保留期是否一致，是否存在数据缺口？；共用匹配池场景下，房间、时段、付费状态、端和版本这五个对比维度当前是否已具备统一、可核对的数据口径？

任务回执：task-2c5a38f0860fd870d06b
