# H5 起源埋点盘点工件

本目录保存 2026-08-28 对 Waje Special 的只读盘点回执与安全结构化索引。

## 内容

- inventory.json：去除 owner、用户明细、敏感字段值和测试载荷后的结构化索引。
- run-receipt.json：观察日期、来源、覆盖量、质量窗口、关键缺口和安全状态。

## 覆盖

- Ares 元事件：34 个。
- Ares H5/安卓H5 页面：74 个；配置显示 178 个模块/事件，其中 25 个页面为 0/0。
- Ares 事件属性：111 个去重字段；索引仅保留 31 个与 H5 分析直接相关的安全字段。
- Ares 虚拟事件：41 个；索引列出 H5/生命周期/交易/游戏常用目录。
- 数据质量窗口：2026-08-21 至 2026-08-27。

## 证据边界

历史需求用于还原设计；Ares 页面用于确认当前配置；数据质量页用于确认聚合接收/入库；来源包含 js 只能作为 H5 线索。模块列表、功能埋点参数和用户属性页当前返回空结果，不等于没有配置。

## 重要口径

Ares 元事件列表中的 PV/PD/MV/MC 数字位于“昨日入库量”列，当前详情未暴露它们为实际 event_id。禁止将这些数字写入 H5 代码；逻辑功能 ID 与真实 Ares 功能 ID 必须分列。

## 刷新流程

读取历史 Markdown/Lark 采集表 → 只读回读 Ares 元事件、页面、模块、功能参数、事件属性和质量页 → 按对象、真实 ID、revision/hash 去重 → 输出变化/无样本/阻塞 → 更新本地 Markdown 和本目录索引 → 更新飞书阅读版 → 刷新知识图谱。

本目录不保存账号、密码、Cookie、Token、用户明细、完整请求内容或 Ares 埋点测试载荷。
