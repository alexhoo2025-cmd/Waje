# 运行快照说明

默认阅读入口是上级目录的 `latest_run.json` 指向的运行。

`firebase-multiplatform-20260827T112000Z-c44037e0` 是当前紧凑汇总版本：9 个查询共返回 127 行聚合结果，包含数据集元数据、端/包汇总和维度 Top 3；没有返回原始事件行或用户/设备明细。

更早的运行保留为审计历史，其中包含较细粒度的聚合排行，不是当前报告入口。后续运行统一使用 `sql/summary/`，结果上限 500 行，并按窗口级汇总优先。
