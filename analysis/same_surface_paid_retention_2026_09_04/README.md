# 三端付费用户同端留存｜阶段交付

## 当前交付与状态

- HTML：`output/html/Waje-全平台用户生命周期与付费价值分析-H5自然新增重点-2026-09-04.html`，沿用用户指定路径。
- 飞书：`https://ksg964l11fam.sg.larksuite.com/docx/DBgEdYvnTojGrmxQ5QUlxGPlgkf`。创建一次，图表修正后回读revision 9，7张表、2张图及关键数值一致。
- Markdown：本目录`report.md`，并同步到原知识存档路径。
- **分析状态为partial，不是全部计划完成。** 近期APP实际端观察已验证；6—8月全量同端回补、首充实际端、H5/PWA识别、同人群LTV仍未完成。
- 原HTML保存在`historical/prior_report.html`；旧查询及原始聚合结果未覆盖。

## 新口径与实际证据

- 用户确认：新增付费=注册当天支付成功；注册实际发生端为起点。首次付费=历史首充实际发生端。主指标同端活跃，APP合并Android+iOS但保留子端。
- 使用服务端支付成功，不使用创建订单；最新画像首端不可回填历史实际端。
- 9月1—3日网页探查已检测键中无可靠PWA/独立运行/WebView标记，不能推广为全部历史永久缺字段。
- 同窗口首充成功事件client_type为0。首充标记按有效时间去重样本不等同于已完整核验历史首充，实际端缺失时不形成三端留存结论。
- 原账号日活只用于总量对账。近期同端查询联合已识别客户端、服务端主动行为、网页及账号日活；未知端不算流失。
- 同端确认比例是观测下界，不代表遥测完备；APP子端迁移不等于APP总层流失。

## 工件与复跑

1. `run_queries.py`维护单次25GiB审计账本，单条5GiB上限；已有结果拒绝覆盖。只在新授权范围内执行SQL，禁止把超限查询拆成多个独立账本绕过限制。
2. `retention_core.py`与`test_retention_core.py`定义/测试20个边界场景；实际SQL由`results/`与`query_ledger.json`追溯。
3. `build_deliverables.py`构建唯一`artifact.json`及Markdown；`package_report.mjs`调用官方可移植读取器、静态SVG导出和验证器，并生成同源飞书图像。
4. 飞书再修改时先fetch现有文档，做局部更新，不重复创建。首次创作临时目录已清理；新草稿须重新执行lark-doc初始化工作流，再更新草稿路径。正式XML存档为`lark_release_candidate.xml`。
5. `validate_deliverables.py`包含25项数据/内容/两格式检查。`browser_verification.json`验证1440/390宽度及来源交互；`lark_verification.json`记录API回读，不声称飞书手机App实际测试。

## 审查

独立Sonnet审查任务`task-c3a990db61c478de7a70`已验收并发布`independent_review.md`。主Agent纠正原审查的N日偏移、APP/子端混淆和LTV语义混用，执行20个测试。未采纳其对偏移方向的错误文字解释。模型意见不作为查询事实。

## 明确尚缺

- 历史全量试算66,205,527,074字节，约61.66GiB，仅dry-run，未执行。
- 完整历史同端结果需认证的按日实际端聚合源，或新的明确查询授权；不能用本次9月观察样本替代。
- 缺PWA运行形态与首充实际端时，不以渠道名称、设备名或最近登录推断。
- 版本/包/设备/金额拆解只覆盖9月2日批次及次日，不能解释月度变动或因果；低于10人的单元不输出。
- 设备型号未转换为高低档；金额四分位是相对排序而非固定价格档位，同金额可跨组。
- 本目录历史背景是按旧画像首平台分组的账号任意端回访，不能与同端观察值做环比。

未修改生产数据、权限、全局模型或常规自动化；未发送外部消息。
