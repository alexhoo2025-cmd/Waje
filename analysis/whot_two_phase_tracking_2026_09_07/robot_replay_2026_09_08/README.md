# 需求16：机器人胜负偏离与整局回放

2026年9月8日增补二期的总体方案、核心数据指标及算法、具体埋点实现。原二期08—15保留，新增16；一期不改。

- 在线机器人策略文档：revision 69，玩法文档：revision 5738；策略生效与参数表不作生产事实推定。
- 飞书二期增补前为revision 22，已含用户修改；通过3次局部插入保留原内容，未整篇覆盖。
- `addition.py`为新增内容源；`build_addition.py`以`before_local`为基线更新原canonical artifact、Markdown、XML及需求合同，保留原block ID和原有正文，只改必要范围说明。
- 重新生成旧两期构建器的产物后，需要执行本目录`build_addition.py`再用统一`deliver_readable_report.mjs`渲染二期；不要仅运行旧构建器而遗漏新增需求16。
- `publish_addition.py`读取最新版本后按章增补并全文回读校验；已有章节只校验，不重复插入。后续编辑用飞书最新block ID定位变更，不覆盖用户批注。
- `publish_receipt.json`记录当前回读revision；`phase2_after.json`为最新回读证据；`finalize.py`更新总回执。
- 原15项验收与本次修改前本地产物保存在`before_local`，可追溯恢复。

3个核心指标：必赢机器人输局率、必输机器人赢局率、偏离局完整回放通过率。7个新逻辑事件，二期共31条事件合同。

当前仅完成设计与文档交付；未采集真实牌局，未部署回放或埋点服务。此轮为已有合同的独立增补，直接复用统一报告构建与局部文档编辑流程。
