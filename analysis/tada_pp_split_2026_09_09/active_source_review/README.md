# 活跃分母专项核验

## 已确认

起源已登录。通过数据管理→虚拟事件只读查看id98 `active_events`：备注明确包含AL、AQ、LOGIN、LOGOUT，与DAU统计口径一致。SQL源为`view_metaevent_active_events`。

元事件管理进一步确认AL为APP启动/后台唤醒，AQ为关闭/调入后台，LOGIN为登录完成/切换账号，LOGOUT为登出；PV另列页面进入。所有编辑面板只读，取消退出，未保存配置。

## 仍待确认

原分母的`realtime_edw_user_version_daily`与以上活跃视图是否同账号集合、如何生成、何种日期时区和事件条件、是否含只有用户信息同步而无行为的账号。当前BigQuery MCP仍返回Auth required；起源网页登录不等于MCP授权恢复。

## 本次报告更正

- 宏观摘要及新老用户段落改用已核验的下注人数、金额说明差异，撤回以未认证活跃分母推导的渠道参与率结论。
- 下注渗透率显示待核，两个图改为已核验的厂商下注人数；旧数值及图保存在本目录备份，不填零、不替换未经验证的新分母。
- 后续先读取视图定义/日表ETL，再用完整38日窗口、相同渠道及年龄口径复算真实活跃账号并与原分母做集合/数量对账。查询先校验和估算，遵循5GiB/次及25GiB/轮限制。
- SQL `next_metadata.sql`仅为下一步已知来源的只读元数据查询，尚未执行。
- 用户级明细不导出；生产表、埋点和起源配置均不修改。

本状态是“部分核实并完成保守更正”，不是“活跃分母已通过认证”。
